name: Sudoku
main: com.comze_instancelabs.sudoku.Main
version: ${minigames.releaseversion}-${minigames.timestamp}
author: [instancelabs, mepeisen]
permissions:
  sudoku.create:
    description: Gives access to create an arena.
commands:
   sudoku:
      description: Main command
      aliases: [su]     
      usage: /<command>
